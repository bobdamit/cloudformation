# Cloudformation Stacks
This is for general infrastructure bits not related to any single app

# Check ECS agent logs
sudo tail -f /var/log/ecs/ecs-agent.log

# Also check the ECS init logs
sudo tail -f /var/log/ecs/ecs-init.log

